# httproute-fe.yaml
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: fe-globaly-app-route
  namespace: globalyapp-web-server
spec:
  parentRefs:
    - name: globalyapp-external-gw
  hostnames:
    - "staging.globalyapp.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: fe-globaly-app-svc
          port: 80
