apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: fe-globaly-app-hc
  namespace: globalyapp-web-server
spec:
  targetRef:
    group: ""
    kind: Service
    name: fe-globaly-app-svc
  default:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 2
    unhealthyThreshold: 3
    config:
      type: HTTP
      httpHealthCheck:
        port: 80
        requestPath: /nginx-health
