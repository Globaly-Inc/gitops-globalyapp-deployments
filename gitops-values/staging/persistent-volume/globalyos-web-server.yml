#namespace: agentcisapp-web-server #### for future reference
storage:
  Name: "globalyapp-web-server"
  size: 20Gi
  accessModes:
    - ReadWriteMany

  nfs:
    host: nfs.agentcis-staging.internal
    path: /nfs-staging/pods-storage/globalyapp-web-server